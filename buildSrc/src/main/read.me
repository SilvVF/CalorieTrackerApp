this module contains the all of the necessary dependency paths and variables
needed for the base gradle file and all other KTS gradle files

to migrate to kotlin gradle use some of these objects
allows for easier changing of dependencies by changing the variable
it is associated with